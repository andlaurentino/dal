# Full-history view: served entirely from the lake.
apiVersion: dal.io/v1alpha1
kind: Projection
metadata:
  name: events-history
spec:
  sources:
    - syncRef: events-to-lake
      view:
        table: events/raw
  queryable:
    defaultLimit: 20
