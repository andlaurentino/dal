apiVersion: dal.io/v1alpha1
kind: Projection
metadata:
  name: events-recent
spec:
  sources:
    - connectionRef: postgres
      view:
        table: user_events
        columns:
          - source: event_id
          - source: user_id
          - source: event_type
          - source: occurred_at
