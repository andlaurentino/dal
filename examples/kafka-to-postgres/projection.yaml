apiVersion: dal.io/v1alpha1
kind: Projection
metadata:
  name: user-events-view
spec:
  sources:
    - syncRef: events-to-warehouse
      maxStalenessSeconds: 30
  queryable:
    table: user_events
    defaultLimit: 100
